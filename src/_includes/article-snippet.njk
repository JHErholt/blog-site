<li>
      <article class="padding">
        <img src="{{ post.data.image }}" alt="{{ post.data.imageAlt }}" class="snippet-img">
        <h3 class="snippet-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        <p class="snippet-meta">by <span>{{ post.data.author }}</span> on <span>{{ post.date | postDate}}</span></p>
        <p class="snippet-description">{{ post.data.description}}</p>
        <a href="{{ post.url }}" class="snippet-btn">Continue Reading</a>
      </article>
</li>